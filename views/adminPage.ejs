<% include header %>

<div class="container" style="margin-top: 5rem">
    <div class="row">
        <div class="col-md-4">
            <h4>Last 50 questions</h4>
            <hr>
            <ul class="list-group"></ul>
            <% question.forEach(function(question){ %>
                <li class="list-group-item"><a href="/question/<%=question._id%>"><%=question.question%></a></li>
            <% }); %>
            </ul>
        </div>
        <div class="col-md-4">
            <%console.log(answer)%>
        </div>
        <div class="col-md-4">3</div>
    </div>
</div>

<% include footer %>